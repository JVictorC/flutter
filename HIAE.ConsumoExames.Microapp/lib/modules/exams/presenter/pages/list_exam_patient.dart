import 'package:flutter/material.dart';

class ListExamPatient extends StatefulWidget {
  const ListExamPatient({Key? key}) : super(key: key);

  @override
  State<ListExamPatient> createState() => _ListExamPatientState();
}

class _ListExamPatientState extends State<ListExamPatient> {
  @override
  Widget build(BuildContext context) => Container();
}
