import 'package:flutter/material.dart';

class FilterEmpty extends StatelessWidget {
  const FilterEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
